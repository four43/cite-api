//Copyright 2012, etc.

(function (root, factory) {
    if (typeof define === 'function' && define.amd) {
        // AMD.
        define(['ajax', 'handlebars'], factory);
    } else {
        // Browser globals
        root.citeApi = factory(root.$, root._);
    }
}(this, function ($, _) {


